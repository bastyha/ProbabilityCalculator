#include <iostream>
#include "Probability.h"

unsigned int factorial(unsigned int num, unsigned int dividedBy = 0);
unsigned int nCr(unsigned int n, unsigned int r);
double division(double a, double b); //don't ask why (for arrays)
double roundToDecimal(double numToRound, int decToRound = 1);